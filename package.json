{
  "name": "googlematrix-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "babel src --extensions \".js,.ts\" --out-dir dist --copy-files --no-copy-ignored",
    "start": "node src/index.js",
    "dev": "ts-node-dev -r tsconfig-paths/register --respawn --ignore-watch node_modules --no-notify src/server.ts",
    "pretest": "NODE_ENV=test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VitorBrangioni/googlematrix-poc.git"
  },
  "author": "Vitor Brangioni",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/VitorBrangioni/googlematrix-poc/issues"
  },
  "homepage": "https://github.com/VitorBrangioni/googlematrix-poc#readme",
  "dependencies": {
    "@googlemaps/google-maps-services-js": "^3.3.10",
    "typescript": "^4.6.2"
  }
}
